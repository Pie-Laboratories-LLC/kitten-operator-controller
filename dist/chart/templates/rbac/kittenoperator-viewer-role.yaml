{{- if .Values.rbac.helpers.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "kitten-operator-controller.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "kitten-operator-controller.resourceName" (dict "suffix" "kittenoperator-viewer-role" "context" $) }}
rules:
- apiGroups:
  - kitten.pielaboratories.com
  resources:
  - kittenoperators
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kitten.pielaboratories.com
  resources:
  - kittenoperators/status
  verbs:
  - get
{{- end }}
