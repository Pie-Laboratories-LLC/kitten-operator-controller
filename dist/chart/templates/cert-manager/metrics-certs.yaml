{{- if and .Values.certManager.enabled .Values.metrics.enabled .Values.metrics.secure }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "kitten-operator-controller.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "kitten-operator-controller.resourceName" (dict "suffix" "metrics-certs" "context" $) }}
  namespace: {{ .Release.Namespace }}
spec:
  dnsNames:
  - {{ include "kitten-operator-controller.resourceName" (dict "suffix" "controller-manager-metrics-service" "context" $) }}.{{ include "kitten-operator-controller.namespaceName" $ }}.svc
  - {{ include "kitten-operator-controller.resourceName" (dict "suffix" "controller-manager-metrics-service" "context" $) }}.{{ include "kitten-operator-controller.namespaceName" $ }}.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: {{ include "kitten-operator-controller.resourceName" (dict "suffix" "selfsigned-issuer" "context" $) }}
  secretName: metrics-server-cert
{{- end }}
